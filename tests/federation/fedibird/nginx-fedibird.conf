# SPDX-FileCopyrightText: misskey-egress-proxy contributors
# SPDX-License-Identifier: AGPL-3.0-only
#
# TLS-terminating front door for the Fedibird peer, the plain remote peer.
# Goes straight to Fedibird — Fedibird is not the system under test.
events {
    worker_connections 1024;
}

http {
    resolver 127.0.0.11 valid=5s;

    server {
        listen 443 ssl;
        server_name _;
        client_max_body_size 10M;

        ssl_certificate /certs/fedibird.crt;
        ssl_certificate_key /certs/fedibird.key;

        location / {
            set $upstream fedibird-web;
            proxy_pass http://$upstream:3000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto https;
        }
    }
}
